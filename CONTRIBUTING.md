# Contributing to Liventra

Thank you for considering contributing to Liventra!

## Development Guidelines
1. All code must adhere to WordPress Coding Standards (`PHPCS`).
2. Maintain strict SOLID architecture boundaries (interfaces, entities, repositories, services, controllers).
3. All code modifications must pass `node tests/run-tests.js`.
4. Run `composer quality` before submitting pull requests.
