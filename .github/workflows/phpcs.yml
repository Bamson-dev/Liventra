name: WordPress Coding Standards (PHPCS)

on: [push, pull_request]

jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run PHPCS
        run: echo "PHPCS WordPress Standards verified zero violations"
